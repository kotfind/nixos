# Writing Claude Code Rules

When the user asks to add a Claude Code rule (`.claude/rules/*.md`), don't copy their words verbatim. Rephrase to be:
- More precise and unambiguous
- Better structured (use headings, lists, examples)
- Consistent with existing rules' tone and format
